<h1>Books</h1>

<ul>
    <% @books.each do |book| %>
        <li>
            <%= link_to book.name, book %> <%= button_to 'Delete', book_path(book.id), method: :delete, data: { confirm: "Are you sure?" } %>
        </li>
    <% end %>
</ul>

<%= button_to 'Home', root_path, method: :get %>
<%= button_to 'Create', new_book_path, method: :get %>